#include <stdio.h>
#include <stdlib.h>
#include <main.h>

int main(int argc,char* argv[]) {
    int myrank, nprocsm i;
    int  *buf;
    MPI_Status status;

    MPI_Init(&argc, &argv);

    MPI_Comm_rank(MPI_COMM)
}